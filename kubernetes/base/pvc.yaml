apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: template-cache-pvc
  namespace: idta-editor
  labels:
    app.kubernetes.io/name: idta-submodel-editor
    app.kubernetes.io/component: storage
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: standard
  resources:
    requests:
      storage: 5Gi
